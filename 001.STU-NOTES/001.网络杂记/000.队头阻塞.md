# 队头阻塞
在计算机网络中，队头阻塞（HOL 阻塞，Head-of-line blocking）指的是一种性能瓶颈：当队列中的第一个数据包被阻塞时，后续数据包即使可以被处理，也会被一同阻塞。

在 HTTP/1.1 中，如果客户端向服务器发送多个请求而不等待响应，服务器会按顺序处理这些请求。如果第一个请求的响应被延迟，后续请求的响应也会被延迟。HTTP/2 通过请求多路复用解决了应用层的队头阻塞，但在传输层（TCP）仍然存在。


## 参考资料
- [队头阻塞](https://developer.mozilla.org/zh-CN/docs/Glossary/Head_of_line_blocking)